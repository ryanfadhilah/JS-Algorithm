

function insertion(arr) {

    let done
    let not_done
    let memory

    for (let i = 0; i < arr.length; i++) {
        // check As dengan angka di depan
        if (arr[i] > arr[i + 1]) {
            memory = ar[i + 1]
            // ar[i + 1] = ar[i]

        }
    }
}

const arr = [-6, 20, 8, -2, 4]
insertion(arr)
console.log(arr)